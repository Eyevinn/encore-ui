# Frontend Configuration
# Copy this file to .env.local and update the values for your environment

# OPTION 1: Unified application (backend serves frontend) - RECOMMENDED
# Use relative path since frontend and backend are served from same origin
VITE_ENCORE_API_URL=/api

# OPTION 2: Separate development servers
# Uncomment the line below if running frontend and backend separately during development
# VITE_ENCORE_API_URL=http://localhost:3001/api

# OPTION 3: Direct connection to Encore API (may have CORS issues)
# Uncomment the line below to connect directly to Encore API
# VITE_ENCORE_API_URL=http://localhost:8080

# Bearer token is now handled by the backend server (when using unified application)
# Only use this if connecting directly to Encore API
# VITE_ENCORE_BEARER_TOKEN=your-token-here